# Julia / Pkg
Manifest.toml

# Local runs (prunner / src/runner.jl, templates)
templates/results/
runner_*.log

# Tests & CI artifacts
*.jl.cov
*.jl.*.cov
*.jl.mem
coverage/
lcov.info

# OS / editor noise
.DS_Store
Thumbs.db
*~
